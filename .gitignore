node_modules/
dist/
dist-ssr/
*.local

# Env files with real secrets — only .env.example is checked in
.env
.env.local
.env.*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Note: public/data/aliases.json is checked in as an empty placeholder
# and regenerated by `npm run fetch-aliases` locally and during CI builds.
# It's intentionally NOT gitignored so the app works on fresh clones.

# Dexie Cloud credentials — NEVER commit these
dexie-cloud.json
dexie-cloud.key
